# Minimal clone config. Native postinstall allowlist lives in package.json
# pnpm.onlyBuiltDependencies (canvas, esbuild) and pnpm-workspace.yaml.
# Do not commit node_modules/.modules.yaml (ignoredBuilds sticky state).
auto-install-peers=true
