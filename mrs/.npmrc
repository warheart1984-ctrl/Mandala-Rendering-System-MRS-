shamefully-hoist=true
auto-install-peers=true
# Allow native postinstall scripts (pnpm 10 blocks them unless listed).
# Prefer package.json pnpm.onlyBuiltDependencies; keep this as a belt-and-suspenders note for clones.
