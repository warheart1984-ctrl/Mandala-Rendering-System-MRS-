# Benchmarks

```bash
node examples/benchmarks/bench-render.mjs
```

Writes `last-run.json` with **measured** Node CanvasRenderer timings only.

Does **not** claim:

- Browser WebGPU throughput
- RT4D path-tracer performance
- Superiority vs other 4D renderers
